// Dùng chung cho mọi trang danh sách có phân trang server-side (xem Pagination.tsx +
// ProductList.tsx làm mẫu) — đổi số lượng/lựa chọn mặc định chỉ cần sửa ở đây.
export const PAGE_SIZE_OPTIONS = [10, 20, 50];
export const DEFAULT_PAGE_SIZE = PAGE_SIZE_OPTIONS[0];
